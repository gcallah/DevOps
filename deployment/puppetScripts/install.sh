# Install boto3 and any required dependencies
pip install boto3

# Install AWS command line too.
pip install awscli --upgrade

# Configure the AWS. Enter the AWS access key id, secret access key and default region when prompted..
aws configure
